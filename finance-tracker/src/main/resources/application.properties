spring.application.name=finance-tracker

spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/finance_db}

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration

spring.webmvc.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:5173}
spring.webmvc.cors.allowed-methods=*
spring.webmvc.cors.allowed-headers=*

app.jwtSecret=${JWT_SECRET:LocalDevelopmentSecretKeyDoNotUseInProduction123456}
app.jwtExpirationMs=${JWT_EXPIRATION_MS:86400000}