# 个人记账与消费分析系统

面向个人用户的收支记录、预算管理和消费分析工具。核心能力是回答四个问题：钱在哪里、花在了什么地方、哪些消费偏离预算、下个月如何调整。

## 语言

**账户（Account）**：
资金的存放位置或分类归属。分两种：实体账户是真实的钱包（支付宝、银行卡、现金），虚拟账户是收支分类的抽象（餐饮支出、工资收入）。
_Avoid_：钱包、分类

**交易（Transaction）**：
一次资金流动的记录，由流出方和流入方组成。包括日常收支和账户间转账。
_Avoid_：流水、账单、记录

**预算（Budget）**：
某个支出分类的月度上限。绑定到 EXPENSE 类型的虚拟账户，永久生效。
_Avoid_：限额、额度

**月报（Monthly Report）**：
某月的收支汇总与消费洞察，实时聚合计算，包括收入、支出、结余、储蓄率、环比变化。
_Avoid_：月度统计、月度报表

**资产账户（Asset Account）**：
用户拥有的资金存放位置，如银行卡、现金、支付宝。余额为正表示用户有钱。
_Avoid_：实体账户、资金账户

**负债账户（Liability Account）**：
用户欠他人的资金，如信用卡、花呗、房贷。余额为正表示用户欠款。
_Avoid_：欠款账户

**权益账户（Equity Account）**：
所有者权益的体现。新用户注册时自动创建 Opening-Balance 账户，用于记录初始资金来源。
_Avoid_：净资产账户

**虚拟账户（Virtual Account）**：
系统内部用于分类的账户，不对应真实资金位置。分两类：EXPENSE（费用类，如餐饮）和 INCOME（收入类，如工资）。
_Avoid_：分类账户

## 关系

- 一个 **账户** 属于一个用户，分五种类型：ASSET、LIABILITY、EQUITY、EXPENSE、INCOME
- 一笔 **交易** 关联两个账户（fromAccountId 和 toAccountId）
- 一个 **预算** 绑定一个 EXPENSE 类型虚拟账户
- 一个 **月报** 聚合一个用户某月的所有交易

## 平账公式

```
Assets + Liabilities + Equity + Income + Expenses = 0
```

- 资产增加记正，减少记负
- 负债增加记正，减少记负
- 权益增加记正，减少记负
- 收入增加记正（代表权益增加）
- 支出增加记正（代表权益减少）

## 示例对话

> **开发者**："用户记一笔 30 元的奶茶支出，后端怎么处理？"
> **领域专家**："前端传 `{ amount: 30, category: '餐饮', accountId: '支付宝ID' }`，后端找到或创建'餐饮'虚拟账户，创建交易 `{ fromAccountId: '支付宝ID', toAccountId: '餐饮虚拟账户ID', amount: 30 }`。"

> **开发者**："查某个账户的余额怎么实现？"
> **领域专家**："实时聚合：`toAccountId == 该账户` 的金额之和减去 `fromAccountId == 该账户` 的金额之和。不存储余额，避免数据不一致。"

> **开发者**："新用户录入支付宝初始余额 5000 元，怎么处理？"
> **领域专家**："创建交易 `{ fromAccountId: 'Equity:Opening-Balance', toAccountId: '支付宝ID', amount: 5000 }`。初始资金来自权益账户，账本才能平。"

> **开发者**："用户用信用卡消费 200 元买奶茶，怎么记录？"
> **领域专家**："创建交易 `{ fromAccountId: '信用卡ID', toAccountId: '餐饮虚拟账户ID', amount: 200 }`。信用卡是负债账户，消费时负债增加。还款时再从银行卡转到信用卡。"

## 扩展概念

**固定支出（Fixed Expense）**：
每月金额相对稳定的支出，如房租、水电、会员。在 Account 上通过 `fixed` 字段标记。
_Avoid_：刚性支出

**必要消费（Necessary Expense）**：
维持基本生活所需的支出，如餐饮、交通、住房。在创建分类时设置默认必要性。
_AVOID_：刚需

**异常消费（Anomalous Transaction）**：
单笔金额超过该分类历史均值 2 倍的交易。创建交易时实时检测并返回警告。
_AVOID_：可疑消费

**导入预览（Import Preview）**：
CSV 导入前的确认步骤，展示待导入记录并标记疑似重复项，用户确认后才真正写入。
_AVOID_：导入确认

## 标记的歧义

- "账户"曾同时指代资金位置和支出分类——已解决：拆分为实体账户和虚拟账户
- "类型"曾指 Transaction 的 type 字段（income/expense）——已解决：复式记账模型下不再需要 type 字段，类型由账户性质决定
- "分类"曾同时指前端常量和后端数据——已解决：分类对应虚拟账户，由 Account 实体管理
